document.getElementById('btn-shopease').addEventListener('click', function (event){
event.preventDefault();
 const assignValue =  getInnerTextById ('assign-head') ;
 
     const sum = assignValue - 1;
 document.getElementById('assign-head').innerText = sum;
//  Completed work at assign value

const navValue = getInnerTextById ('nav-value');
console.log(navValue)
const navalSum = navValue + 1;
document.getElementById('nav-value').innerText = navalSum;
// nav value added

alert('Board Updated successfully');

 document.getElementById('btn-shopease').disabled = true;
// .style.pointerEvents = "none";
//                     cardElem.style.opacity = "0.5";

const container = document.getElementById('add-div-after-click')
const p = document.createElement('p');
p.innerText = `
      You have completed the task fix mobile button issue at 1:26:53 pm
     `
     container.appendChild(p)
})

// Shopease btn complere

document.getElementById('btn-cloudsync').addEventListener('click', function (event){
     event.preventDefault();
      const assignValue =  getInnerTextById ('assign-head') ;
      
          const sum = assignValue - 1;
      document.getElementById('assign-head').innerText = sum;
     //  Completed work at assign value
     
     const navValue = getInnerTextById ('nav-value');
     console.log(navValue)
     const navalSum = navValue + 1;
     document.getElementById('nav-value').innerText = navalSum;
     // nav value added
     
     alert('Board Updated successfully');

     const container = document.getElementById('add-div-after-click')
const p = document.createElement('p');
p.innerText = `
      You have completed the task fix mobile button issue at 1:26:53 pm
     `
     container.appendChild(p)
      
     })
document.getElementById('btn-switpay').addEventListener('click', function (event){
     event.preventDefault();
      const assignValue =  getInnerTextById ('assign-head') ;
      
          const sum = assignValue - 1;
      document.getElementById('assign-head').innerText = sum;
     //  Completed work at assign value
     
     const navValue = getInnerTextById ('nav-value');
     console.log(navValue)
     const navalSum = navValue + 1;
     document.getElementById('nav-value').innerText = navalSum;
     // nav value added
     
     alert('Board Updated successfully');
     const container = document.getElementById('add-div-after-click')
const p = document.createElement('p');
p.innerText = `
      You have completed the task fix mobile button issue at 1:26:53 pm
     `
     container.appendChild(p)
      
     })
document.getElementById('btn-meta').addEventListener('click', function (event){
     event.preventDefault();
      const assignValue =  getInnerTextById ('assign-head') ;
      
          const sum = assignValue - 1;
      document.getElementById('assign-head').innerText = sum;
     //  Completed work at assign value
     
     const navValue = getInnerTextById ('nav-value');
     console.log(navValue)
     const navalSum = navValue + 1;
     document.getElementById('nav-value').innerText = navalSum;
     // nav value added
     
     alert('Board Updated successfully');
     const container = document.getElementById('add-div-after-click')
const p = document.createElement('p');
p.innerText = `
      You have completed the task fix mobile button issue at 1:26:53 pm
     `
     container.appendChild(p)
      
     })
document.getElementById('btn-googlllc').addEventListener('click', function (event){
     event.preventDefault();
      const assignValue =  getInnerTextById ('assign-head') ;
      
          const sum = assignValue - 1;
      document.getElementById('assign-head').innerText = sum;
     //  Completed work at assign value
     
     const navValue = getInnerTextById ('nav-value');
     console.log(navValue)
     const navalSum = navValue + 1;
     document.getElementById('nav-value').innerText = navalSum;
     // nav value added
     
     alert('Board Updated successfully');

     const container = document.getElementById('add-div-after-click')
const p = document.createElement('p');
p.innerText = `
      You have completed the task fix mobile button issue at 1:26:53 pm
     `
     container.appendChild(p)
      
     })
document.getElementById('btn-glassdor').addEventListener('click', function (event){
     event.preventDefault();
      const assignValue =  getInnerTextById ('assign-head') ;
      
          const sum = assignValue - 1;
      document.getElementById('assign-head').innerText = sum;
     //  Completed work at assign value
     
     const navValue = getInnerTextById ('nav-value');
     console.log(navValue)
     const navalSum = navValue + 1;
     document.getElementById('nav-value').innerText = navalSum;
     // nav value added
     
     alert('Board Updated successfully');

     const container = document.getElementById('add-div-after-click')
const p = document.createElement('p');
p.innerText = `
      You have completed the task fix mobile button issue at 1:26:53 pm
     `
     container.appendChild(p)
      
     })

     

     // function changeBackground() {
     //      const colors = ["violet","tomato","lightblue", "skyblue", "lightgreen", "hotpink", "lightorange","lightpink","cyan","bisque"];
     //      document.body.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
     //  }

      document.getElementById('circle-btn').addEventListener('click', function () {
          const colors = ["violet", "tomato", "lightblue", "skyblue", "lightgreen", "hotpink", "lightpink", "cyan", "bisque"];
          document.body.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
          console.log('circle paichi')
     });

     // add external file
     document.getElementById("add-extarnal").addEventListener("click", function(){
          window.location.href="./login.html"
      })


      document.getElementById("clear-history").addEventListener("click", function(event){
          event.preventDefault();
          document.getElementById('add-div-after-click').style.display = 'none'
      })
     //  clear history